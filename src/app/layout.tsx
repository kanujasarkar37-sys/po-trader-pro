import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PO Trader Pro — Pocket Option Auto-Trading Bot",
  description:
    "Auto-trading bot for Pocket Option: live SSID connection, multi-indicator confluence signals with CALL/PUT direction and precise timing, demo & real account automation, balance tracking, backtesting and AI market analysis.",
  keywords: [
    "Pocket Option", "binary options", "auto trading bot", "trading signals",
    "CALL PUT signals", "SSID", "OTC pairs", "binary bot",
  ],
  authors: [{ name: "PO Trader Pro" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export const viewport: Viewport = {
  themeColor: "#09090b",
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-zinc-950 text-zinc-100`}
      >
        {children}
      </body>
    </html>
  );
}
